"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.s = void 0;
const react_native_1 = require("react-native");
const mainStyles_1 = require("./mainStyles");
const s = (classes, ...args) => {
    const flattenClasses = classes
        .reduce((pv, cv, i) => {
        return `${pv}${cv}${i !== args.length ? args[i] : ''}`;
    }, '') // Merge classes list with arg list
        .replace(/\s+/g, ' ') // Remove extra spaces
        .trim()
        .split(' ')
        .reduce((pv, cv) => [...pv, mainStyles_1.mainStyles[cv]], []);
    return react_native_1.StyleSheet.flatten(flattenClasses);
};
exports.s = s;
