export declare type BackgroundColorClass = `bg-${string}-${string}` | `bg-${string}`;
export declare type BackgroundColorStyles = {
    [key: string]: {
        backgroundColor: string;
    };
};
export declare const buildBackgroundColorStyles: () => BackgroundColorStyles;
