import { defualtSizes } from './sizes';
export declare type MaxHeightStyle = {
    [key in MaxHeightClass]: {
        maxHeight: string | number;
    };
};
export declare type MaxHeightClass = `max-h-${keyof typeof defualtSizes}`;
export declare const buildMaxHeights: () => MaxHeightStyle;
