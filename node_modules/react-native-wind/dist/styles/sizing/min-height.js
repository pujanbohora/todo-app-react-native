"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.buildMinHeights = void 0;
const sizes_1 = require("./sizes");
const buildMinHeights = () => {
    const minHeight = {};
    Object.keys(sizes_1.sizes).forEach((s) => {
        minHeight[`min-h-${s}`] = {
            minHeight: sizes_1.sizes[s],
        };
    });
    return minHeight;
};
exports.buildMinHeights = buildMinHeights;
