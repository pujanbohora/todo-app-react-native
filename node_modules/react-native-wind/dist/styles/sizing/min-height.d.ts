import { defualtSizes } from './sizes';
export declare type MinHeightStyle = {
    [key in MinHeightClass]: {
        minHeight: string | number;
    };
};
export declare type MinHeightClass = `min-h-${keyof typeof defualtSizes}`;
export declare const buildMinHeights: () => MinHeightStyle;
