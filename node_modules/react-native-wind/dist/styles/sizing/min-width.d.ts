import { defualtSizes } from './sizes';
export declare type MinWidthStyle = {
    [key in MinWidthClass]: {
        minWidth: string | number;
    };
};
export declare type MinWidthClass = `min-w-${keyof typeof defualtSizes}`;
export declare const buildMinWidths: () => MinWidthStyle;
