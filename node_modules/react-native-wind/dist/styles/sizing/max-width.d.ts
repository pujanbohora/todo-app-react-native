import { defualtSizes } from './sizes';
export declare type MaxWidthStyle = {
    [key in MaxWidthClass]: {
        maxWidth: string | number;
    };
};
export declare type MaxWidthClass = `max-w-${keyof typeof defualtSizes}`;
export declare const buildMaxWidths: () => MaxWidthStyle;
